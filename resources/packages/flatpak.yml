################################################################################
# IMPORTANT                                                                    #
# (1) This manifest YAML only is meant for appending the file installations    #
#     in the AutomataCI's generated manifest file.                             #
# (2) It is only be appended after the modules: field where you should only    #
#     list the array items to be installed into the flatpak packages.          #
# (3) This means it works hand-in-hand with your                               #
#     PACKAGE::assemble_flatpak_content function for assembling custom files   #
# (4) Please exclude the main binary and appdata.xml installations as these    #
#     elements are generated automatically by AutomataCI.                      #
################################################################################
#modules:
#  - name: file-here
#    buildsystem: simple
#    build-commands:
#      - install -D YOUR_COPIED_FILE /app/share/DESTINATION
#    sources:
#      - type: file
#        path: YOUR_COPIED_FILE
